services:
  query-api:
    profiles: [debugged-locally]

  web:
    depends_on: !reset {}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./nginx.frontend.debug.conf:/etc/nginx/conf.d/default.conf:ro
